"""ADB interaction contracts.

Platform-specific observation contracts are exposed from ``adb.observation``.
ADB server and transport administration contracts are exposed from
``adb.management``. Import contracts from the narrowest capability namespace
that owns them.
"""
