"""Desktop-window interaction contracts.

Platform-specific observation contracts are exposed from
``desktop_window.observation``. Window state and geometry administration
contracts are exposed from ``desktop_window.management``. Import contracts from
the narrowest capability namespace that owns them.
"""
