"""Optional plug-in packages for the interaction capability framework.

Extensions depend on public core contracts and are enabled only when a consuming
application imports and composes them. Core packages must not import extensions.
"""
