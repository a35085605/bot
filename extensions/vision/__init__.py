"""Bundled opt-in vision extension.

The extension contains reusable visual reference-asset and template-matching
capabilities. Importing the interaction framework core does not load this package;
consumers enable it explicitly through ``extensions.vision`` imports and their own
composition root.
"""
